// Если создается функция (void Sum) обязательно надо её(Sum) указать, либо программа не булет работать

int Sum(int a, int b)
{
    Console.Write(a + b);
    return a + b;
}
int a = 5;
int b = 4;
Sum(a, b);
